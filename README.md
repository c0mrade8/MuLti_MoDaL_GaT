# MuLti_MoDaL_GaT
model_main.py contains the pre execution architecture code.
model_main2.py contains the latest continuation of the work along with the latest result achieved after modifying the execution code with speeding up of computation.
Other files contain the code segments to contruct the graph, preprocessing of the RAVEDESS dataset.
